apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcp-client.fullname" . }}-config
  labels:
    {{- include "mcp-client.labels" . | nindent 4 }}
data:
  # Example: any non-sensitive static files or config can go here
  # For instance, if the client had a client_config.json, you could put:
  # client_config.json: |
  #   {
  #     "timeout": 30,
  #     "max_retries": 5
  #   }
